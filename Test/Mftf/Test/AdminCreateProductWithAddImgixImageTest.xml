<?xml version="1.0" encoding="UTF-8"?>

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminCreateProductWithAddImgixImageTest">
        <annotations>
            <features value="Catalog"/>
            <stories value="Create a Simple Product via Admin and Add imgix Image"/>
            <title value="Create a Simple Product via Admin and Add imgix Image"/>
            <description value="Save API key in system configuration and Create Simple Product with imgix image"/>
            <severity value="CRITICAL"/>
            <testCaseId value=""/>
            <group value="imgixImage"/>
        </annotations>
        <before>
            <!-- Admin login -->
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginToAdminPanel"/>
            <!-- Open IMGIX system configuration section and save values -->
            <actionGroup ref="NavigateToDefaultLayoutsImgixSettingActionGroup" stepKey="navigateToImgixConfigurationPage"/>
            <!-- Save system configuration -->
            <actionGroup ref="AdminSaveConfigActionGroup" stepKey="saveConfig"/>
            <magentoCLI stepKey="runCacheFlush" command="cache:flush"/>
            <!-- Create default category -->
            <createData entity="_defaultCategory" stepKey="createPreReqCategory"/>
        </before>
        <after>
            <!-- Delete data -->
            <deleteData createDataKey="createPreReqCategory" stepKey="deletePreReqCategory"/>
            <actionGroup ref="DeleteProductBySkuActionGroup" stepKey="deleteProduct">
                <argument name="sku" value="{{_defaultProduct.sku}}"/>
            </actionGroup>
            <actionGroup ref="ClearFiltersAdminDataGridActionGroup" stepKey="clearFilter"/>
            <!-- Reset Imgix Settings and save system configuration -->
            <actionGroup ref="NavigateToDefaultLayoutsImgixSettingResetActionGroup" stepKey="navigateToImgixConfigurationPageResetSettings"/>
            <actionGroup ref="AdminSaveConfigActionGroup" stepKey="resetSettingsAndSaveConfig"/>
            <!-- Admin logout -->
            <actionGroup ref="AdminLogoutActionGroup" stepKey="amOnLogoutPage"/>
        </after>
        <!-- Create simple product start -->
        <actionGroup ref="FillAdminSimpleProductFormWithImgixImageActionGroup" stepKey="fillProductFieldsInAdmin">
            <argument name="category" value="$$createPreReqCategory$$"/>
            <argument name="simpleProduct" value="_defaultProduct"/>
        </actionGroup>
        <magentoCLI stepKey="runCronIndex" command="cron:run --group=index"/>
        
        <actionGroup ref="AssertProductInStorefrontCategoryPage" stepKey="assertProductInStorefront1">
            <argument name="category" value="$$createPreReqCategory$$"/>
            <argument name="product" value="_defaultProduct"/>
        </actionGroup>
        <actionGroup ref="AssertProductInStorefrontProductPageActionGroup" stepKey="assertProductInStorefront2">
            <argument name="product" value="_defaultProduct"/>
        </actionGroup>
        <!-- Create simple product end -->
    </test>
</tests>